/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://atharianr.dev', // Ganti sama domain kamu
    generateRobotsTxt: true,          // Generate robots.txt otomatis
    sitemapSize: 7000,                // Default aman
    changefreq: 'monthly',            // Sesuai kebutuhan
    priority: 0.7,                    // Prioritas crawling
  };
  